## Archived

This directory contains the retired TypeScript daemon implementation.

- It is retained as a historical reference only.
- It is no longer part of the supported build, CI, or release workflows.
- The supported daemon lives in `cmd/looperd`.
