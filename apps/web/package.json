{
  "name": "@looper/web",
  "private": true,
  "description": "Archived placeholder TypeScript workspace package retained for reference only.",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target browser",
    "typecheck": "bun x tsc -p tsconfig.json --noEmit"
  }
}
