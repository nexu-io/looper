{
  "name": "@powerformer/looper",
  "version": "0.2.1",
  "private": true,
  "description": "Archived legacy TypeScript CLI snapshot retained for reference only.",
  "type": "module",
  "engines": {
    "node": ">=20",
    "bun": ">=1.3.12"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run scripts/build.ts",
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test src"
  },
  "dependencies": {
    "cac": "^6.7.14"
  }
}
