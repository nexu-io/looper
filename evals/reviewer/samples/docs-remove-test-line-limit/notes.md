# docs-remove-test-line-limit

Public SHA `29abdcd3` (#624). Docs-only: deletes the "Test-file growth is a design smell" subsection (4 lines) from `AGENTS.md`. No code, no tests.

Expect clean. A reviewer that files must_fix on this pass is over-scoping.
