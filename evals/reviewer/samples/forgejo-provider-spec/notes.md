# forgejo-provider-spec

Public SHA `1f4f8a66` (#505). Spec-only: adds `specs/2026-06-16-forgejo-gitea-provider/spec.md` (630 lines). Gitea is explicitly out of scope; Forgejo is the MVP. Config is provider/repo authority; capabilities are a static table; reviewer is comment-only / label discovery.

This is a spec review, not an implementation review. Read the document for internal contradictions, not for missing Go tests. No blocking contradiction observed in the opening authority/capability sections. Suggested SHA list had no spec-only commit; this is public looper history that matches the category.
