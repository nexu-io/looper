# app-identity-sandbox-test-only

Public SHA `ef14149f` (#660). Pure test change: `internal/diffanchor/diffanchor_test.go` (+2) and `internal/e2e/github_app_identity_sandbox_test.go` (+4/-1). No production files. Suggested SHA list had no test-only commit; this is public looper history that matches the category.

No remaining defect observed. A test-only pass should stay clean unless the tests themselves assert the wrong contract.
